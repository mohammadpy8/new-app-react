import React from 'react';

const ContactDetails = (props) => {
    console.log(props);
    return (
        <div>
            <p>user name is : </p>
            <p>user email is :</p>
        </div>
    );
};

export default ContactDetails;